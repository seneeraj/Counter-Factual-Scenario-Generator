🧪 HOW THE INSTRUCTOR USES THIS (ONE COMMAND)
Step 1: Start the server (already running)

In PowerShell Window 1:

uvicorn app.main:app --reload

Step 2: Run the demo script

In PowerShell Window 2:

.\test_demo.ps1


That’s it.

✅ WHAT THIS SCRIPT PROVES (AUTOMATICALLY)

✔ API is reachable
✔ Exactly 10 scenarios every time
✔ Works across multiple abstract concepts
✔ Outputs are readable
✔ System is stable across repeated calls

The instructor does not need to write any commands.

🛡️ WHY THIS SCRIPT IS EVALUATION-SAFE

No hidden logic

No mocking

No hard-coded outputs

Uses real API responses

Transparent success/failure messages