## Evaluation Steps for Visitor:

1. Open PowerShell inside the project folder
2. Install dependencies:
   pip install -r requirements.txt
3. Start the server:
   python -m uvicorn app.main:app --reload
4. In a second terminal, run:
   .\test_demo.ps1
That’s it.


WHAT THIS SCRIPT PROVES (AUTOMATICALLY)

✔ API is reachable
✔ Exactly 10 scenarios every time
✔ Works across multiple abstract concepts
✔ Outputs are readable
✔ System is stable across repeated calls


In Case any Evaluation requires manual  testing:
Run below command in a seperate Power shell:

Invoke-RestMethod -Method Post `
  -Uri "http://127.0.0.1:8000/generate?concept=Cultural Drift"
And then:
Run  .\test_demo.ps1

