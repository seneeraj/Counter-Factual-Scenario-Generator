def generate_scenarios(concept: str):
    # TEMP STUB — replaced after fine-tuning
    return [
        {"id": i+1, "text": f"What if {concept} changed in way #{i+1}?"}
        for i in range(10)
    ]
